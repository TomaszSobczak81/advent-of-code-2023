pub struct Day2;

impl crate::aoc::Compute for Day2 {
    fn compute_part_one(&self, version: String) -> String {
        let _input = crate::aoc::input_load("2".to_string(), "1".to_string(), version.clone());

        return "0".to_string();
    }

    fn compute_part_two(&self, version: String) -> String {
        let _input = crate::aoc::input_load("2".to_string(), "2".to_string(), version.clone());

        return "0".to_string();
    }
}
